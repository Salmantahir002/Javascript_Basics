// Class 01: Variables

const accoundId = 14455
let accoundEmail = "Salmantahir@gmail.com"
var accountPassword = "12235"
let accoundState


//accoundId = 23 // Not allowed because the value first stored using const.
// And any value stores in const cannot be changed later.

// We should always use let and const instead of var because var has issue with the
// block scope and functional scope.

console.table([accoundId, accoundEmail, accountPassword, accoundState]);
