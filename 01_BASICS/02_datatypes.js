"Use Strict"; //treat All the Javascript code as newer version

//alert("Hello") // alert function will not popup because we are using nodejs instead of browser console

let name="Salman"   //String Datatype

let age = 21        //Number Datatype

let isLoggedIn=false  //Boolean Datatype

let state = null

//null is a standalone value

/* null is a special type when we assign null to a variable it means 
that we are Intentionally put null instead of undefined.
It is also used when we dont need 0 value in our program.*/

// Interview question related to Null:
// When we do console.log(Typeof null) what will be the output
// Answer: It will give us object as the datatype

console.log(typeof undefined);// undefined
console.log(typeof null); //object

//symbol => for Unique value purpose



